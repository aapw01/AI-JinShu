id: literary
name: Literary Strategy
description: Literary writing strategy focused on depth, consistency, and style.
stages:
  architect:
    provider: __default__
    model: __default__
  outliner:
    provider: __default__
    model: __default__
  writer:
    provider: __default__
    model: __default__
  reviewer:
    provider: __default__
    model: __default__
  finalizer:
    provider: __default__
    model: __default__
