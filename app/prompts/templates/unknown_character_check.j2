你是小说连续性审校员。以下是本章正文中出现的、不在预设角色表中的命名角色。
请判断每个角色的出现是否合理。

已知角色表：{{ roster | join("、") }}

需要判断的角色：{{ unknown_names | join("、") }}

前序章节摘要（最近{{ recent_summaries|length }}章）：
{% for s in recent_summaries %}
第{{ s.chapter_num }}章：{{ s.content | default(s.summary | default("")) }}
{% endfor %}

第{{ chapter_num }}章正文（节选）：
{{ draft[:2000] }}

对每个需要判断的角色，输出以下 JSON：
{
  "verdicts": [
    {
      "name": "角色名",
      "result": "unreasonable 或 new_reasonable 或 minor",
      "reason": "判断理由（一句话）",
      "evidence": "正文中相关原文片段（10字以内）",
      "confidence": 0.0到1.0之间的数字
    }
  ]
}

判断标准：
- unreasonable：有名字、承担剧情功能，但与前文逻辑无关或突兀出现
- new_reasonable：合理引入的新角色（场景需要、有明确来历、不破坏前文逻辑）
- minor：路人、群众、无实质剧情功能

confidence < 0.65 的情况统一输出 minor。
