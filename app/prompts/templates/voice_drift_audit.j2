你是角色"声纹漂移"审计员。给定某角色的近 N 章对白片段与其历史指纹，
评估当前章节对白是否偏离该角色稳定声纹。

声纹维度：
- avg_sentence_len：平均句长（字数）。
- formality_score：0~1，正式度。
- voice_register：``high``（书面/敬语）/``neutral``/``low``（口语/俚语）/``mixed``。

输出要求：
- 对新片段重新算这三个维度。
- 与历史指纹比对，给出 ``drift_score`` (0~1)。
- 当 drift_score > threshold 时 ``triggered=true``，并在 ``diff_dimensions``
  列出偏离最大的 1-3 个维度。

仅输出 JSON。

# 历史指纹
{{ historical_fingerprint }}

# 当前章节对白片段
{{ current_dialogues }}

# 触发阈值
threshold = {{ threshold }}
character_key = {{ character_key }}
