你是大纲修订师。当前章节大纲在一致性硬门控（character_existence /
hard_constraint）上失败，请仅修订与 blocker 直接相关的字段，**保持其它字段
原样**。修订必须满足：

- 不引入新角色 / 新设定（除非 blocker 明确要求）。
- 不改变章节核心目标（chapter_objective）的"方向"，只允许在描述层修字。
- 不要扩写至原长度的 1.3 倍以上。

# 当前章节大纲（JSON）
{{ outline_json }}

# 一致性硬门控 blockers
{% for b in blockers %}
- 类别：{{ b.category }}；说明：{{ b.message }}
{% endfor %}

# 第 {{ attempt_no }} 次修订（最多 {{ max_attempts }} 次）

输出要求（仅 JSON）：
- ``revised_outline``：修订后大纲对象（与输入同 schema，仅修订必要字段）。
- ``changed_fields``：列出本次修订真实改动的顶级字段名（数组）。
- ``rationale``：≤ 80 字描述本次修订思路。
