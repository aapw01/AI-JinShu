{# chapter_body_contract.j2
   目标：把章节正文输出严格收敛到单字段 JSON，供 llm_contract.py 做 schema 校验。
   防护：禁止解释性前后缀、禁止标题回显、禁止输出额外键，避免结构化输出在工程侧不可消费。
   面试可讲：prompt 只是第一层约束，真正兜底还依赖 Pydantic schema + contract retry。
#}
<output_contract>
你必须只返回一个 JSON 对象，且仅包含以下字段：
{
  "chapter_body": "章节正文字符串"
}

严格禁止：
1) 任何前言/后记/解释/总结（例如"以下是根据反馈…"、"重点解决如下问题…"）。
2) Markdown 代码块、Markdown 标题、列表符号、加粗标记（如 ``` / ## / - / **）。
3) 正文第一行输出章节标题（如"第X章 ..."）。
4) 复述输入标签块中的原文（<global_bible>、<outline_json>、<annotations_json> 仅供参考）。
5) 输出除 chapter_body 之外的任何键。
6) 输出 HTML 标签（如 <p>、<br>）或 Markdown 语法。

字数要求（必须达标）：
- 理想区间：{{ ideal_min_word_count or 2200 }} 到 {{ ideal_max_word_count or 2800 }} 字；可接受区间：{{ min_word_count or 2000 }} 到 {{ soft_max_word_count or 3000 }} 字。
- 本章正文不得少于 {{ min_word_count or 2000 }} 字；不足视为未完成，必须继续扩写直到达标。
- 若超过 {{ soft_max_word_count or 3000 }} 字，应优先压缩重复心理描写、重复动作回放与解释性复述。
- 尽量不要超过 {{ hard_ceiling_word_count or 3500 }} 字；收紧字数时禁止删主冲突、关键兑现、章末钩子或改变剧情走向。

排版格式（严格遵守）：
- 所有段落之间统一使用两个换行符（\n\n）分隔，无一例外。
- 避免连续出现大量极短段；关键段可以短，但整体段落长度要有层次感。
- 对话可以独立成段，但不要机械地把每一句都拆成单独一段。
- 动作描写、心理描写、环境描写按叙事节奏分段，避免无意义碎切。
- 单句段只用于强调关键动作、情绪或悬念，不可泛滥。
- 禁止在 chapter_body 中出现连续三个及以上换行符。

番茄节奏要求：
- 对话段占全章段落的 35~55%，避免大段纯叙述堆砌。
- 每 2~4 段必须出现推进动作（冲突升级、信息揭示、决策转折之一）。
- 章末最后 1~3 段必须制造悬念钩子，让读者想翻下一章。
- 少量单句段落可以制造"呼吸感"，但整章不能被大量 20~40 字碎段切散。

写作目标：
- 以叙事段落直接开场，保持剧情连续性与角色动机一致。
- 必须提供新的有效推进，不得只把上一章已经成立的结论换个说法再讲一遍。
- 若上一章结尾留下明确动作/离场/受伤/冲突后果，本章开头必须承接，不能无过渡跳回不可能状态。
- 保持自然小说文风，不要解释你在执行规则。
- 充分展开场景描写、人物对话和心理活动，确保内容饱满。
</output_contract>
