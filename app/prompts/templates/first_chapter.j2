{# v2 first chapter prompt: task semantics + shared contract include #}
你是长篇网文作者，请创作第1章正文。

<global_bible>
{{ context.global_bible | default("无") }}
</global_bible>

<outline_json>
{
  "chapter_num": 1,
  "title": {{ (outline.title | default("")) | tojson_pretty }},
  "outline": {{ (outline.outline | default("")) | tojson_pretty }},
  "role": {{ (outline.role | default("")) | tojson_pretty }},
  "purpose": {{ (outline.purpose | default("")) | tojson_pretty }},
  "foreshadowing": {{ (outline.foreshadowing | default("无")) | tojson_pretty }},
  "hook": {{ (outline.hook | default("")) | tojson_pretty }},
  "chapter_objective": {{ (outline.chapter_objective | default("")) | tojson_pretty }},
  "required_new_information": {{ (outline.required_new_information | default([])) | tojson_pretty }},
  "required_irreversible_change": {{ (outline.required_irreversible_change | default("")) | tojson_pretty }},
  "relationship_delta": {{ (outline.relationship_delta | default("")) | tojson_pretty }},
  "conflict_axis": {{ (outline.conflict_axis | default("")) | tojson_pretty }},
  "payoff_kind": {{ (outline.payoff_kind | default("")) | tojson_pretty }},
  "reveal_kind": {{ (outline.reveal_kind | default("")) | tojson_pretty }},
  "forbidden_repeats": {{ (outline.forbidden_repeats | default([])) | tojson_pretty }},
  "opening_scene": {{ (outline.opening_scene | default("")) | tojson_pretty }},
  "opening_character_positions": {{ (outline.opening_character_positions | default([])) | tojson_pretty }},
  "opening_time_state": {{ (outline.opening_time_state | default("")) | tojson_pretty }},
  "transition_mode": {{ (outline.transition_mode | default("")) | tojson_pretty }}
}
</outline_json>

{% if context.book_progression_state %}
<book_progression_state_json>
{{ context.book_progression_state | tojson_pretty }}
</book_progression_state_json>
{% endif %}

{% if context.anti_repeat_constraints %}
<anti_repeat_constraints_json>
{{ context.anti_repeat_constraints | tojson_pretty }}
</anti_repeat_constraints_json>
{% endif %}

<writing_goals>
- 理想字数：{{ ideal_min_word_count or 2200 }}-{{ ideal_max_word_count or 2800 }}；可接受 {{ min_word_count or 2000 }}-{{ soft_max_word_count or 3000 }}；目标中心 {{ target_word_count or word_count or 2600 }}；尽量不超过 {{ hard_ceiling_word_count or 3500 }}。
- 语言：{{ language or "zh" }}
- 风格标签：{{ native_style_profile or "默认" }}
- 黄金开篇：前 300 字内必须出现主角与核心困境，禁止静态背景说明开篇。
- 前三章抓力：第一章必须明确核心矛盾，让读者感知"这个故事值得追"。
- 至少埋下 2 个可兑现钩子（一个章末悬念 + 一个暗线伏笔）。
- 人物行为必须可追溯到动机，不允许"为推动剧情而行动"。
- 短句推进 + 关键处句长变化，保持信息密度与可读性兼顾。
- 必须完成 `chapter_objective` 指向的首章推进，至少建立一个不可逆变化或强承诺。
- 禁止把设定说明当正文推进，必须通过冲突、行动和选择把设定带出来。
- 若字数超出可接受区间，优先合并重复心理、动作回放和解释性复述，不要牺牲核心爽点与章末钩子。
</writing_goals>

<continuity_priority>
1. 角色状态一致（身份、能力、关系、当前目标）
2. 事件因果闭环（行动 -> 反应 -> 后果）
3. 文风稳定（语气、节奏、叙事视角）
</continuity_priority>

<style_metrics>
- 节奏：每 2~4 段出现一次显著推进（冲突升级/信息揭示/决策转折）。
- 句式：以短句推进，关键情绪段落允许长句拉伸，但避免整章句长单一。
- 冲突：本章至少有 2 处显性阻力，不得"平铺直叙到结尾"。
- 钩子：章末必须留下可在后续章节兑现的行动性悬念。
- 对话：对话段占全章段落 35~55%，避免大段纯叙述堆砌。
- 段落：保持移动端快节奏阅读感，但不要把有效内容切成大量极短段。
</style_metrics>

<forbidden_behavior>
- 参考材料仅用于理解，不得复述标签块原文。
- 禁止把"设定说明/创作计划/人工指令"写入正文。
- 禁止输出章节标题行与解释性前言。
- 禁止用大段背景说明替代有效剧情推进。
</forbidden_behavior>

{% include "chapter_body_contract.j2" %}
